<h1> Blogs </h1>

<% @blogs.each do |blog| %>
  <hr>
  <h2><%= blog.title %></h2>
  <h3><%= blog.subtitle %></h3>
  <p><%= blog.body %></p>
  <%= link_to 'Show', blog_path(blog) %>
  <%= link_to 'Edit', edit_blog_path(blog) %>
<% end %>

<%= link_to 'New Blog', new_blog_path %>